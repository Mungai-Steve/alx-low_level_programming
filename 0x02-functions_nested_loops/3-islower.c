#include "main.h"

/**
 * _islower - checks if int c is lower
 * @c: - character to be checked
 * Return: 1 if islower 0 if is not lower
 */

int _islower(int c)
{

	if (c >= 97 && c <= 122)
{
	return (1);
}
{
	return (0);
}
}
