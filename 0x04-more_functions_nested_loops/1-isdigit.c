#include "main.h"

/**
 * _isdigit - chech if c is digit
 *
 * @c: integer to be checked
 *
 * Return: 1 if c is digit else return 0
 */

int _isdigit(int c)

{
	while (c <= 0 && c >= 9)


	{
		return (1);
	}



	{
		return (0);
	}
}
